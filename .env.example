# Database (SQLite)
DATABASE_URL="file:./db/custom.db"

# Admin login (http://localhost:3001/admin)
ADMIN_PASSWORD="admin123"
ADMIN_SECRET="change-me-in-production"

# Optional: better photo invoice recognition (OpenAI-compatible vision).
# Without a key, photo inbound falls back to local OCR (tesseract).
# INBOUND_OCR_API_KEY="sk-..."
# INBOUND_OCR_BASE_URL="https://api.openai.com/v1"
# INBOUND_OCR_MODEL="gpt-4o-mini"
